# Core service and LLM clients
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.0,<3.0
python-multipart>=0.0.9
PyYAML>=6.0
openai>=1.0,<3.0
requests>=2.31

# GIS data retrieval and web-map raster preview
numpy>=1.24
Pillow>=10.0
geopandas>=0.14
fiona>=1.9
shapely>=2.0
rasterio>=1.3
sentinelhub>=3.10

# Testing and development
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27

# QGIS and GDAL are installed through the selected QGIS/OSGeo distribution.
# Do not add them here: their compatible binaries are platform-specific.
